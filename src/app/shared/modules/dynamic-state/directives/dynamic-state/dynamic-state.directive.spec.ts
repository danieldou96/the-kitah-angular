import { DynamicStateDirective } from './dynamic-state.directive';

describe('DynamicStateDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicStateDirective();
    expect(directive).toBeTruthy();
  });
});
